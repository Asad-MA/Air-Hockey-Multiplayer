<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/global-styles.css">
    <link rel="stylesheet" href="/styles/dashboard.css">
    <title>CHAT | Air Hockey Online</title>
</head>
<style>
   .chat-window-wrap.flex-grow {
    width: 70%;
}

.chat-users {
    background: #201f26;
    width: 100%;
    max-width: 300px;
}

.chat-window-wrap {
    background: #201f26;
    width: 70%;
    border-radius: 20px;
}
</style>
<body>
    <%- include('../partials/header') %>

        <div class="dashboard-layout d-flex gap-20 p-20">
            <div class="ui-sidebar sidebar-left w-20per ">
                <%- include('../partials/sidebar-left') %>
            </div>

            <div class="dashboard-content w-80per md-w-80per loading p-20 d-flex direction-col gap-30  pos-relative flex-grow">
                <div class="chat-wrapper d-flex gap-20 flex-grow">
                    <div class="chat-users chat-sidebar">
                        users
                    </div>
                    <div class="chat-window-wrap flex-grow">
                        <div class="chat-window">chat-window</div>
                        <div class="chat-sender-box">sendbox</div>
                    </div>
                </div>
            </div>

            
        </div>

        <%- include('../partials/footer') %>

        <script defer type="module" src="/js/pages/friends.js"></script>
</body>
</html>